# Changelog

## 0.1.0

- Initial release: libSQL MCP server on database-mcp-core (Python), local
  files and remote libSQL/Turso servers with auth token support, read-only
  by default (PRAGMA query_only), row caps, statement timeouts.
