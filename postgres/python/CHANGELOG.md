# Changelog

## 0.1.0

- Initial release: PostgreSQL MCP server on database-mcp-core (Python),
  psycopg driver, read-only by default (default_transaction_read_only
  startup parameter), row caps, server-side statement_timeout.
