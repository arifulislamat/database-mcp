# Changelog

## 0.1.0

- Initial release: SQLite MCP server on database-mcp-core (Python), stdlib
  sqlite3 driver, read-only by default (read-only open plus
  PRAGMA query_only), row caps, statement deadline.
